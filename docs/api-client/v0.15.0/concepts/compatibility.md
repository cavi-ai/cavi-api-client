---
documentedVersion: 0.15.0
---

# Compatibility

These pages are locked to the packed `@cavi-ai/api-client@0.12.0` declaration surface. A development-branch symbol is unavailable here unless the artifact contains it. Provider support is conditional on the upstream runtime and reported capabilities.

This client mirrors and verifies upstream-compatible behavior. Upstream runtimes remain the canonical protocol owners.
