---
documentedVersion: {{documentedVersion}}
---
# Migration and version support

This client mirrors and verifies upstream-compatible behavior. Upstream runtimes remain the canonical protocol owners.

Version `{{documentedVersion}}` is the sole documentation set in this artifact. Pin the package version, verify capabilities at runtime, and review provider-specific upstream changes before upgrading. No compatibility guarantee is made for symbols absent from the locked public export manifest.
