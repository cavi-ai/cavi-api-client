---
documentedVersion: 0.11.0
---

# React

The stable `frameworks/react` subpath supplies gateway context bindings. See the compile-checked [React gateway example](../../../../examples/react-gateway.tsx). Pass authentication from your application boundary.

This client mirrors and verifies upstream-compatible behavior. Upstream runtimes remain the canonical protocol owners.
