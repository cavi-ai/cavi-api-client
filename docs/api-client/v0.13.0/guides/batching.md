---
documentedVersion: 0.13.0
---

# Batching

Batching is optional rather than universally available in v0.12.0. The compile-checked [batch capability guard](../examples/runtime-capabilities.ts) checks both the advertised `batch` capability and `submitBatch`.

This client mirrors and verifies upstream-compatible behavior. Upstream runtimes remain the canonical protocol owners.
