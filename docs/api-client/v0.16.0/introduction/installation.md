---
documentedVersion: 0.16.0
---

# Installation

Install the exact documented release with `pnpm add @cavi-ai/api-client@0.16.0`. The package is ESM and includes TypeScript declarations. Keep provider API keys on a trusted backend.

This client mirrors and verifies upstream-compatible behavior. Upstream runtimes remain the canonical protocol owners.
