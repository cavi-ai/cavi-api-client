# Architecture

The durable architecture guidance now lives in [README.md](README.md):

- Layered ownership and dependency direction: see "Layered architecture".
- Provider plugin boundaries: see "Adding a gateway provider".
- Transport, media, wiki, React, adapter, team-manifest, and path contracts:
  see the matching sections under "Reference".
- Enforced package boundaries: see "Development" and
  `src/__tests__/package-hardening.test.ts`.

This file is kept only as a stable pointer for direct links.
